version: '3.8'

services:
  simply-ip-vault:
    build: .
    container_name: simply_ip_vault
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=sqlite://data/simply_ip_vault.db?mode=rwc
      - RUST_LOG=info
    volumes:
      - ./data:/app/data
    restart: unless-stopped
